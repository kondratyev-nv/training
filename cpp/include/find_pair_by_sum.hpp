#ifndef FIND_PAIR_BY_SUM_HPP
#define FIND_PAIR_BY_SUM_HPP

#include <utility>
#include <vector>

std::pair<int, int> find_pair_by_sum(std::vector<int> const& values,
                                     int target_sum);

#endif  // FIND_PAIR_BY_SUM_HPP
