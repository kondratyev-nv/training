
#include <tuple>
#include <vector>

#include "optional.hpp"

optional<std::pair<size_t, size_t>> find_subarray_with_sum(std::vector<unsigned int> const& values,
                                                           unsigned int target);
