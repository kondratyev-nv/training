#ifndef CAN_PERMUTE_FOR_SUM
#define CAN_PERMUTE_FOR_SUM

#include <vector>

bool can_permute_for_sum(std::vector<int> const& a,
                         std::vector<int> const& b,
                         int k);

#endif  // CAN_PERMUTE_FOR_SUM
