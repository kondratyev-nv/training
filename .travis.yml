matrix:
  include:
    - language: python
      python: 3.6
      before_script:
        - cd python
      script:
        - python3 -m unittest discover -v --pattern *_tests.py
    - language: java
      before_script:
        - cd java
      jdk:
        - openjdk8
